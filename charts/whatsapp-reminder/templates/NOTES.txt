WhatsApp Reminder has been installed!

Your CronJob is scheduled to run: {{ .Values.schedule }}

To check the status of your CronJob:
  kubectl get cronjob {{ include "whatsapp-reminder.fullname" . }} -n {{ .Release.Namespace }}

To see the jobs created by the CronJob:
  kubectl get jobs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

To view logs from the most recent job:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} --tail=50

{{- if and (not .Values.secrets.serviceAccountJson) (not .Values.secrets.serviceAccountJsonBase64) }}

WARNING: No service account JSON provided. The application will not be able to access Google Sheets.
Please provide the service account JSON via one of these methods:
  --set secrets.serviceAccountJsonBase64=<base64_encoded_json>
  OR
  --set-file secrets.serviceAccountJson=./service-account.json
  OR
  --set secrets.serviceAccountJson='{"type":"service_account",...}'
{{- end }}

{{- if not .Values.secrets.emailOriginAddress }}

WARNING: No email origin address provided. The application will not be able to send emails.
Please provide the email address via:
  --set secrets.emailOriginAddress=your-email@example.com
{{- end }}

{{- if not .Values.secrets.emailOriginName }}

WARNING: No email origin name provided.
Please provide the email name via:
  --set secrets.emailOriginName="Your Name"
{{- end }}

{{- if not .Values.config.googleSheets.spreadsheetId }}

WARNING: Google Sheets spreadsheet ID is not configured.
Please set it in values.yaml or via --set config.googleSheets.spreadsheetId=YOUR_ID
{{- end }}
